side-effects-cache=false
ignore-scripts=false
# onnxruntime-node's postinstall downloads ~250MB of CUDA GPU binaries on
# linux x64; nothing loads onnxruntime-node at runtime (browser uses
# onnxruntime-web), so skip it everywhere.
onnxruntime-node-install-cuda=skip